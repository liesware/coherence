https://www.cryptopp.com/wiki/Keys_and_Formats

[lies@localhost ~]$ openssl ecparam -list_curves
  secp256k1 : SECG curve over a 256 bit prime field
  secp384r1 : NIST/SECG curve over a 384 bit prime field
  secp521r1 : NIST/SECG curve over a 521 bit prime field
  prime256v1: X9.62/SECG curve over a 256 bit prime field

openssl ec -in key.der -inform DER -pubin -out keyout.pem -outform PEM
openssl ec -in key.der -inform DER -pubin -text -noout

echo "Coherence pubkey ec" | xxd -r -p - | openssl ec -inform der -pubin -noout -text

echo "Coherence pubkey rsa" | xxd -r -p - | openssl rsa -inform der  -pubin  -noout -text
echo "Coherence privkey rsa"| xxd -r -p - | openssl rsa -inform der -noout -text

echo "Coherence pubkey ec" | xxd -r -p - > key.der 
xxd -p key.der  | tr -d "\n"
